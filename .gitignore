node_modules
dist
.next
out
.firebase
*.log

# Local-only overrides (never commit these)
.env.local
.env.*.local

# .env.production and .env.development are intentionally tracked in Git
# Cloudflare Pages reads them at build time
