name = "brainvare-api"
main = "src/index.js"
compatibility_date = "2024-09-01"

# ── D1 Database (enquiries) ──────────────────────────────
[[d1_databases]]
binding = "DB"
database_name = "brainvare-db"
database_id = "41551d14-3fe0-4b09-89f3-a53e6d6ecc81"

# ── KV Namespace (content + pages) ──────────────────────
[[kv_namespaces]]
binding = "KV"
id = "cb24419df64942bb86bacfb8c89043ec"

# ── R2 Bucket (video reels) ─────────────────────────────
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "brainvare-cdn"

# Secrets (set via `wrangler secret put`):
# - ADMIN_EMAIL
# - ADMIN_PASSWORD_HASH
# - JWT_SECRET
